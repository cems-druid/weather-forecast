import React from 'react';
import { Typography } from '@mui/material';

const HomePage: React.FC = () => {
    return (
        <div>
            <Typography variant="h4">Welcome to Weather Forecast App</Typography>
            <Typography variant="body1">Use the navbar to explore the features.</Typography>
        </div>
    );
};

export default HomePage;
